[project]
name = "bot_df"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "contracts",
    "observability"
]

[tool.uv.sources]
contracts = { workspace = true }
observability = { workspace = true }
